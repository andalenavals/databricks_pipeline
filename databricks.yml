# yaml-language-server: $schema=bundle_config_schema.json
bundle:
  name: databricks_pipeline

artifacts:
  default:
    type: whl
    path: . # Builds local code into a .whl automatically on deploy
    build: python -m build --wheel
    
include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true